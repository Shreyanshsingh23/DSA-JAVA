import java.util.ArrayList;

public class twoRepeated_element_inarray {
    public static void main(String[] args){
        // boolean [] bool = new boolean [4];
        ArrayList<Boolean>  bool = new ArrayList<>();
         System.out.println(bool);
        
//         int arr[] = {1,2,1,3,4,3};
//         int n = 4;

//            boolean[] visited = new boolean[n + 2];  // defalut value of each element of boolean array is false, we are taking advantage of it

//     int[] result = new int[2];
//     int index = 0;
    
//     for (int i = 0; i < arr.length; i++) {
//         if (visited[arr[i]]) {  // in first appearrance of the number the value will be false only it's obvious, but
//             result[index++] = arr[i]; 
//         } else {
//             visited[arr[i]] = true;   // we are setting it to true, so that if the number appears twice, it will be caught 
//         }
//     }
    
//   //  return result;
//   System.out.println(Arrays.toString(result));
    }

    }

